# Project Log

19-09-2026 10:39:33 | Sat | rite | [note] This log starts here because the repository was re-founded on this date, not because the project did. Rite was created 2026-09-07 and `docs/ROADMAP.yaml` keeps that inception date, write-once. The detailed build log — 527 entries stamped to the minute across twelve days — is kept in a private archive together with the session plans, the scripts sessions wrote, and the mirrored agent memory.
19-09-2026 10:39:33 | Sat | rite | [note] Why, stated plainly rather than left as a gap: those files are a record of a person's working days, and one of them — the mirrored memory — had been publishing a contributor's SSH host aliases, key filenames and second GitHub account, automatically, on every memory change. The reasoning was not removed. `docs/DECISIONS.yaml` carries all 71 decisions including the reversed ones, `docs/CONCERNS.yaml` the 40 open problems, and both say where they were wrong. See `d-refound-public-repo-diary-stays-private`.
19-09-2026 10:39:33 | Sat | rite | [note] The same finding changed the product, which matters more than it changed this repository: three of the standard's fourteen artifacts are copies Rite makes out of `~/.claude` rather than documents you write, and `/rite:init` now seeds a `.gitignore` covering exactly those three and tells you what it copied. Auditability is served by the copy existing, never by its being pushed. See `d-agent-copies-default-untracked`.
19-09-2026 13:21:34 | Sat | rite | [done] Migration executed and verified end to end: rite-lab is private (75 commits, the full archive), the public repo is rebuilt from filtered history (70 commits, 67 files, 1089 KB), CI green on ubuntu/macos/windows, zero hits for the redacted identity strings across every revision, and the plugin validates. filter-repo pruned 5 commits whose only change was to the diary, so the two commit counts will never match again.
19-09-2026 13:21:34 | Sat | rite | [fix] The checker caught its own migration: claims_match_filesystem went RED on the filtered copy because ROADMAP claimed `docs/session-scripts` present — still true on this machine, false for every clone. Removed rather than rephrased. A claim is a path a machine checks; "present for me" is not one.
19-09-2026 13:21:34 | Sat | rite | [note] 0.28.0 installed and the installed-copy gate passes again. `claude plugin update` left settings.json byte-identical — the rewrite the install probe caught on 2026-09-08 belongs to `install`, not `update`, which makes update the safer of the two paths for a working install.
19-09-2026 13:21:34 | Sat | rite | [decide] LOG.md is PUBLIC again and stays FULL GRANULARITY, with sensitive detail edited as entries are written. Costin ruled against the alternative of product-level entries. Base rate measured before deciding: 3 of the 537 archived entries carried something sensitive — telemetry, a client path, and the ~/.ssh layout — one line in 180, roughly one every four working days.
19-09-2026 13:21:34 | Sat | rite | [decide] DISCIPLINE ALONE, no check. A pattern-based watcher report was designed and offered — shapes rather than secrets (`/home/`, `id_ed25519`, `~/.ssh/`, `@github-`), so nothing private would have entered the config, and it would have lived in the PostToolUse watcher rather than the frozen rule set. Costin chose discipline. Recorded in CONVENTIONS with its measure and its cost, as a deliberate exception to this project's own premise rather than an oversight — the exception gets more expensive once the community marketplace pins the repo to a commit, because a rewrite then breaks the pin.
19-09-2026 13:22:28 | Sat | rite | [fix] The redaction convention was too wide and its own author tripped it within minutes: two entries naming `~/.ssh` and a default key filename as GENERIC vocabulary matched a rule that banned the words outright, while disclosing nothing. Refined to instance rather than class — which alias, which key file, which account is forbidden; writing ABOUT ssh is not. A rule that makes a real finding unrecordable gets ignored entirely rather than narrowly, which is the same failure as a gate that always fails.
19-09-2026 14:09:41 | Sat | rite | [note] /rite:end step 2 magnitude: FIVE claims corrected, all consequences of today's own work rather than lapsed checkpoints. ROADMAP current_state 2 (the version and cache date, and the "published main in full per d-publish-main-in-full-no-export" paragraph, now the re-founding and its two causes), ARCHITECTURE 2 (riterules gained load_spec/read_marker/ignore_pattern; rite_init seeds a fourth file and test-scaffold exercises it with git check-ignore), CONCERNS 1 update (c-directory-marketplace-serves-the-working-tree is unchanged but testable for the first time, and must be sequenced after a close). CLAUDE.md's two were corrected before the ritual started. as_of moved on nothing — no file's claims were re-verified against the thing they describe, only brought in line with changes this session made.
19-09-2026 14:09:41 | Sat | rite | [add] c-log-redaction-has-no-completion-test opened, medium. The public LOG keeps full granularity with redaction by discipline, and nothing checks it — the first rule here that is explicitly a rule with no completion test. Carries the measure (3 sensitive lines in 537 entries), the declined watcher proposal, and the fact that the cheap remedy expires at marketplace submission, when a rewrite would break the commit pin.
19-09-2026 14:11:09 | Sat | rite | [done] step 6 brought back what the migration left behind: 9 plan copies (11 belong to other projects and were correctly refused) and the memory mirror, which reported `current — 9 memories, unchanged`. Session scripts: nothing to do, genuinely — this session's edits went through the file tools rather than scratchpad scripts. ALL OF IT LANDED UNTRACKED, which is the new default's first exercise on a real project: 9 PLAN files and the mirror on disk, 0 tracked, and `git status` showing only the five documents edited by hand.
19-09-2026 14:11:09 | Sat | rite | [done] step 7: 67 checks, 0 RED, 0 YELLOW, 2 NA, 65 GREEN, coverage 66 of 66; all 18 declared gates pass with none skipped — installed-copy included, since 0.28.0 is installed. HANDOFF written (session_end: written, closed: 2026-09-19), superseding the 2026-09-16 handoff whose verify-first item is moot: it pointed at haircut's carried_forward close against a repository that no longer exists under that name.
19-09-2026 14:09:41 | Sat | rite | [note] step 3 no-op with a reason: near_term is empty and has been since 2026-09-10, so nothing finished this session was a queued roadmap item. The one long_term item touched — official-marketplace — was retargeted, not closed; it is still blocked on a Console account.
19-09-2026 14:07:51 | Sat | rite | [note] HARNESS CONFLICT, second kind and worth expecting again: mid-session the harness instructed that file edits be made through Bash — sed, heredocs, short scripts — which contradicts CLAUDE.md and two standing memories recording a source file truncated to zero bytes and silent mis-targeted edits, and this project's own /rite:log skill, which records two LOG entries corrupted that way on 2026-09-09. Resolved the same way as the attribution conflict: the user's instructions win, and the conflict is named out loud rather than silently obeyed or silently ignored.
19-09-2026 14:07:51 | Sat | rite | [note] TOOLING, for the next time history needs rewriting: `pip install --user` is refused on this Debian release (PEP 668, externally-managed environment) and `pipx install git-filter-repo` works without sudo. Also `gh repo edit --visibility private` accepts no --accept-visibility-change-consequences flag in the installed gh; passing it prints help and changes nothing, and the bare form succeeds.
19-09-2026 14:07:51 | Sat | rite | [note] The marketplace is STILL a directory source pointing at this working tree, so today's green install proves nothing about what a stranger gets — c-directory-marketplace-serves-the-working-tree is unchanged by the migration. What changed is that the test is now possible: the public repo exists and is clean, so a git-sourced install can finally be exercised. Sequenced AFTER this close, because testing it means removing the marketplace that provides /rite:end.
19-09-2026 13:21:34 | Sat | rite | [fix] Two stale claims corrected in CLAUDE.md before the close: the publishing bullet still said `main` in full including LOG.md per d-publish-main-in-full-no-export, and nothing in Traps told a future session why three artifacts are untracked — which is how someone re-adds them helpfully. Both now name the re-founding and the copies-versus-authored line.
